apiVersion: networking.k8s.io/v1
kind: Ingress
metadata: {name: agent-api}
spec:
  rules:
    - host: workforce.example.invalid
      http:
        paths:
          - path: /
            pathType: Prefix
            backend: {service: {name: agent-api, port: {number: 80}}}
